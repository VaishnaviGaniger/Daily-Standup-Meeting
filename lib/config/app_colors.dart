import 'package:flutter/material.dart';

class AppColors {
  // static const Color rich_teal = Color(0xFF4C00C2);
  // static const Color dark_teal = Color(0xFF6E00FF);
  // static const Color darkest_teal = Color(0xFF7B2FF7);
  static const Color rich_teal = Color(0xFF2E8B7F);
  static const Color dark_teal = Color(0xFF1F5F5B);
  static const Color darkest_teal = Color(0xFF0D4F47);

  static const Color white = Colors.white;
  static const Color whiteLogin = Color.fromRGBO(
    255,
    255,
    255,
    0.8,
  ); // alpha:0.8

  static const Color greyLogin = Color.fromARGB(
    171,
    103,
    102,
    102,
  ); // same as grey[300]

  static const Color transparent = Colors.transparent;

  static const Color purple = Colors.deepPurple;

  static const Color note_color = Color.fromARGB(255, 255, 85, 0);
}
